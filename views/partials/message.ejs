<% if(error != "") { %>
<div class="alert alert-warning">
  <strong><%= error %></strong>
</div>
<% } %>

<!-- break -->

<% if(error_msg != "") { %>
<div class="alert alert-warning">
  <strong><%= error_msg %></strong>
</div>
<% } %>

<!-- break -->
<% if(success_msg != "") { %>
<div class="alert alert-warning">
  <strong><%= success_msg %></strong>
</div>
<% } %>
